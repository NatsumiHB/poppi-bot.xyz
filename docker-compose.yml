version: "3"

services:
    poppi-bot:
        image: pierrezemb/gostatic
        volumes:
            - ./:/srv/http
        networks:
            -  proxy_net

networks:
    proxy_net:
        external: true
